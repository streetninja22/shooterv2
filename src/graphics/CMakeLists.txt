message("Getting source files in ${SOURCE_DIR}")

file(GLOB HEADERS "${SOURCE_DIR}/*.h")
file(GLOB SOURCES "${SOURCE_DIR}/*.cpp")

set(HEADERS_GRAPHICS ${HEADERS} PARENT_SCOPE)
set(SOURCES_GRAPHICS ${SOURCES} PARENT_SCOPE)


